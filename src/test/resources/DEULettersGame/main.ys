
var arr  = [];

for var i = 0 ; i< 100; i++ do
    arr.push(Random.nextInt(1,999));
end

arr = arr.sort((a,b) => do
    if a > b then do return 1; end
    elif b > a then do return -1 ; end
    return 0;
end);

arr.forEach((x, i , old) => do
    println x;
end);
